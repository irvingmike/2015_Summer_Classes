////////// Lab 6 //////////

let firstName = "Fred"
println("\(firstName) is \(count(firstName)) characters long.")

var characterSum = 0
for name in names {
    characterSum += count(name)
}
println(characterSum)

//
